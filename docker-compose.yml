version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: 
      - .env
    ports:
      - "${WEB_PORT}:${WEB_PORT}"
    volumes:
      - ./data:/app/data
    environment:
      - DEV_MODE=true
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${WEB_PORT}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    profiles:
      - prod

  web-dev:
    extends: web
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    profiles:
      - dev

  updater:
    build:
      context: .
      dockerfile: Dockerfile.updater
    volumes:
      - ./data:/app/data
    env_file: .env
    environment:
      - DEV_MODE=true
    profiles:
      - scheduled
    depends_on:
      - web

volumes:
  data: 